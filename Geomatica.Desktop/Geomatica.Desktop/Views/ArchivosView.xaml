<UserControl x:Class="Geomatica.Desktop.Views.ArchivosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:Geomatica.Desktop.Views">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Barra de filtros (se conserva) -->
        <views:BarraFiltrosView Grid.Row="0" Grid.Column="0"
                            DataContext="{Binding Filtros}" />

        <!-- Lista de proyectos -->
        <ListView Grid.Row="0" Grid.Column="1"
              ItemsSource="{Binding Items}"
              SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Título" DisplayMemberBinding="{Binding Nombre}" />
                    <GridViewColumn Header="Fecha"  DisplayMemberBinding="{Binding Fecha}" />
                    <GridViewColumn Header="Dpto"   DisplayMemberBinding="{Binding CodDpto}" />
                    <GridViewColumn Header="Mpio"   DisplayMemberBinding="{Binding CodMpio}" />
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Estado -->
        <TextBlock Grid.Row="1" Grid.ColumnSpan="2"
               Text="{Binding Estado}" Margin="8" />
    </Grid>
</UserControl>
